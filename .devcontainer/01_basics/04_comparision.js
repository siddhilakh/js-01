//console.log(null > 0)
//console.log(null == 0)
//console.log(null >= 0)
// equality check (==) and comparisons(like <,>,>=,etc) are treated differently
// comparisons convert null to a number treating it as zero
console.log("2" == 2)